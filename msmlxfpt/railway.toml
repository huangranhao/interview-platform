# Railway 配置文件
[build]
builder = "dockerfile"

[deploy]
startCommand = ""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
APP_ENV = "production"
DEBUG = "false"

[[services]]
name = "interview-platform"
